<project name="jtcgen" default="compilar">

  <target name="compilar">
    <delete dir="bin" />
    <mkdir dir="bin" />

    <javac srcdir="src/br" destdir="bin" >
      <classpath>
        <fileset dir="lib">
          <include name="*.jar" />
        </fileset>
      </classpath>
    </javac>
  </target>
  <target name="empacotar" depends="compilar">
    <mkdir dir="jar" />
    <jar destfile="jar/jtcgen.jar" basedir="bin"/>
  </target>
</project>